import { darkPalette } from '@/src/styles/palette';
import mediaQuery from '@/src/styles/mediaQuery';
import size from '@/src/styles/size';

const darkTheme = {
  mediaQuery,
  size,
  palette: darkPalette
};

export default darkTheme;
