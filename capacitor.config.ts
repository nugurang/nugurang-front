import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nugurang.nugurang',
  appName: 'nugurang',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
